# HTTP 서버 (도메인 없이 IP로 접속)
server {
    listen 80;
    server_name _;  # 모든 도메인/IP 허용

    location / {
        proxy_pass http://symtalk-be:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $server_name;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 도메인 발급 후 HTTPS 설정 (현재 주석 처리)
# server {
#     listen 80;
#     server_name symtalk.duckdns.org;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl;
#     server_name symtalk.duckdns.org;
#     access_log off;
#
#     ssl_certificate /etc/letsencrypt/live/symtalk.duckdns.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/symtalk.duckdns.org/privkey.pem;
#
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://symtalk-be:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Host $server_name;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
